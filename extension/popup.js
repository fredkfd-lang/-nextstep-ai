const $=id=>document.getElementById(id);
const status=msg=>$('status').textContent=msg;
async function getProfile(){return (await chrome.storage.local.get('beroppProfile')).beroppProfile||null}
$('import').addEventListener('click',async()=>{const file=$('profileFile').files[0];if(!file){status('Choose your exported profile JSON first.');return}try{const p=JSON.parse(await file.text());if(!p||typeof p!=='object'||Array.isArray(p)){throw new Error('Invalid profile')}await chrome.storage.local.set({beroppProfile:p});status('✅ Profile imported into this extension.');}catch(e){status('❌ Invalid JSON profile file.')}});
$('fill').addEventListener('click',async()=>{const p=await getProfile();if(!p){status('Import a profile first.');return}const [tab]=await chrome.tabs.query({active:true,currentWindow:true});if(!tab?.id){status('No active page found.');return}try{const result=await chrome.tabs.sendMessage(tab.id,{type:'BEROPP_FILL',profile:p});const missing=result?.unmatchedFields||[];status(`Filled: ${result?.filled||0} · Not matched: ${missing.length}`);$('missing').textContent=missing.length?`Not matched: ${missing.join(', ')}`:'All available profile fields were matched.';}catch(e){status('This page does not allow autofill. Try a normal website tab.');$('missing').textContent='';}});
$('clear').addEventListener('click',async()=>{await chrome.storage.local.remove('beroppProfile');status('Extension profile deleted.');$('missing').textContent='';});
(async()=>{const p=await getProfile();if(p)status('Profile ready. Click Fill form when you are on an application page.');})();
